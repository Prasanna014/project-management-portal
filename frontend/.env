# Active config for Windows UI -> Linux backend API
VITE_API_BASE_URL=http://57.154.241.153:8080/api
VITE_DEFAULT_USER_ID=1
VITE_USE_TASK_DETAILS_MOCK=false

# ------------------------------------------------------------------
# Server push profile (copy/uncomment later when you are ready)
# Keep only one VITE_API_BASE_URL active at a time.
# Example public API URL after backend deployment:
# VITE_API_BASE_URL=http://<SERVER_OR_PUBLIC_IP>:8080/api
# ------------------------------------------------------------------

